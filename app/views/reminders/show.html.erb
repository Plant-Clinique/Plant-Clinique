<p id="notice"><%= notice %></p>

<div >
  <div class="row" style = "height: 430px;">
    <div class = "col-3">
    </div>
    <div class="col-md-6 bg-rem" style="height: 400px; background-color: green; border: 5px solid green; border-radius: 25px;text-align: center; color:yellow;">
      <div class = "row text-center" style = "height: 50px;"> </div>
      <h1 class='text-center' > Your Reminder </h1>
            <p >
              <strong>User plant:</strong>
              <%= @reminder.user_plant_id %>
            </p>

            <p>
              <strong>User:</strong>
              <%= @reminder.user_id %>
            </p>

            <p>
              <strong>Tick time:</strong>
              <%= @reminder.tick_time %>
            </p>

            <p>
              <strong>Interval:</strong>
              <%= @reminder.interval %>
            </p>

            <p>
              <strong>Description:</strong>
              <%= @reminder.description %>
            </p>

            <p>
              <strong>Reminder Type:</strong>
              <%= @reminder.reminder_type %>
            </p>
        <button class="bg-warning btn rounded"> 
            <%= link_to 'Edit', edit_reminder_path(@reminder) %>
        </button>
        <button class="bg-warning btn rounded"> 
            <%= link_to 'Back', reminders_path %> 
        </button>
    </div>
  </div>
</div>

<div style="height:355px;"> </div>
